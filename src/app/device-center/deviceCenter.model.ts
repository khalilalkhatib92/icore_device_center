export interface deviceCenterCreationDTO{
    name: string;
    address: string;
    latitude: number;
    longitude: number;
}

export interface deviceCenterDTO{
    name: string;
    address: string;
    latitude: number;
    longitude: number;
}